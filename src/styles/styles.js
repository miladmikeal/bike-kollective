import { StyleSheet } from 'react-native';

const globalStyles = StyleSheet.create({
  formContainer: {
    marginVertical: '25%',
    marginLeft: 10,
    marginRight: 10,
    padding: 20,
  },
  mapContainer: {
    flex: 1,
  },
});

export default globalStyles;
