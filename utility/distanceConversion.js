// Converts the given distance in kilometers to miles
export function kmToMile(distanceKm) {
    return distanceKm * 0.621371;
}

// Converts the given distance in miles to kilometers
export function mileToKm(distanceMiles) {
    return distanceMiles * 1.60934;
}